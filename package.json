{
  "name": "workout",
  "version": "2.0.0",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.18.1",
    "dotenv": "^6.1.0",
    "ejs": "^2.5.7",
    "express": "^4.15.4",
    "mongojs": "^2.4.1"
  },
  "scripts": {
    "nodemon": "nodemon server.js NODE_ENV=DEV",
    "watch_client": "cd angular-src && npm run watch",
    "start": "node server.js NODE_DEV=PROD",
    "build_client": "cd angular-src && npm install && npm run prod",
    "postinstall": "npm run build_client"
  },
  "engines": {
    "node": "7.10.1",
    "npm": "4.2.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.4"
  }
}
